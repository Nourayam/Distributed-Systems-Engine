// Create a LogViewer React component that:

// Accepts an array of RAFT events (e.g. elections, partitions, recoveries)

// Displays a scrollable event timeline

// Groups or color-codes entries by event type

// Uses monospace font for log legibility

// (Optional) Adds filters by event category