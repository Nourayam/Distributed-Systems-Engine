// Build a ControlPanel React component that:

// Renders buttons to Start, Stop, and Reset the simulation

// Allows input for: node count, max time, chaos mode toggles

// Sends requests to Flask via Axios or fetch (/raft/start, /raft/control)

// Contains chaos buttons for injecting partition or node failure

// Can be displayed as a right sidebar or bottom panel