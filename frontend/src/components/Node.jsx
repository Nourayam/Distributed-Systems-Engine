// Write a Node React component that:

// Renders an individual node in the RAFT cluster

// Accepts props for ID, state, term, and status (e.g. failed/partitioned)

// Applies appropriate styles and classes based on RAFT state:

// LEADER = gold

// FOLLOWER = blue

// CANDIDATE = amber

// FAILED = gray / red ring

// Shows node metrics on hover via a tooltip