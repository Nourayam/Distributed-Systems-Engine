// Build a ClusterCanvas React component that:

// Displays the RAFT nodes in a circle or ring layout

// Accepts a nodes prop (array of node objects) and renders each with their ID, state, and term

// Highlights the leader (via glow or icon)

// Styles failed or partitioned nodes distinctly

// Optionally supports animated message arrows using SVG or canvas